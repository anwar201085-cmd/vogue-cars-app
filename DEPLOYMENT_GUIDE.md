# دليل نشر تطبيق Vogue Cars 🚀

تهانينا! تطبيقك الآن جاهز للنشر على المتاجر العالمية. اتبع الخطوات التالية لرفع التطبيق بنجاح:

## 1. متطلبات أساسية
*   **حساب مطور جوجل (Google Play Console)**: لنشر نسخة الأندرويد.
*   **حساب مطور أبل (Apple Developer Program)**: لنشر نسخة الآيفون.
*   **مشروع Firebase**: لربط قاعدة البيانات والإشعارات.

## 2. ربط Firebase (خطوة هامة جداً)
1.  اذهب إلى [Firebase Console](https://console.firebase.google.com/).
2.  أنشئ مشروعاً جديداً باسم `Vogue Cars`.
3.  أضف تطبيق Android: استخدم الـ Package Name `com.voguecars.app`.
4.  قم بتحميل ملف `google-services.json` وضعه في المجلد `android/app/`.
5.  أضف تطبيق iOS: استخدم الـ Bundle ID `com.voguecars.app`.
6.  قم بتحميل ملف `GoogleService-Info.plist` وضعه في المجلد `ios/Runner/`.

## 3. النشر على متجر جوجل بلاي (Android)
1.  استخدم ملف الـ **App Bundle (.aab)** الموجود في مجلد `build/app/outputs/bundle/release/`.
2.  ارفعه في قسم "Production" داخل Google Play Console.
3.  املأ بيانات التطبيق (الوصف، الصور، التصنيف).

## 4. النشر على متجر أبل (iOS)
1.  افتح مجلد `ios` باستخدام برنامج **Xcode** على جهاز Mac.
2.  اختر `Product > Archive`.
3.  بعد انتهاء الأرشفة، اختر `Distribute App` واتبع الخطوات لرفعه إلى App Store Connect.

## 5. الدعم الفني
إذا واجهت أي مشكلة تقنية أثناء الرفع، يمكنك العودة إليّ وسأقوم بمساعدتك فوراً.

---
**VOGUE CARS - القيادة نحو المستقبل**
